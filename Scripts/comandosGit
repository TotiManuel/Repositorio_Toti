…o crea un nuevo repositorio en la línea de comando

echo "# Repositorio-Henry" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/TotiManuel/Repositorio-Henry.git
git push -u origin main

…o inserte un repositorio existente desde la línea de comando

git remote add origin https://github.com/TotiManuel/Repositorio-Henry.git
git branch -M main
git push -u origin main

mostrara cambios en el repo actual
git status

confirmar los cambios
git add ARCHIVO

guardar cambios en archivo local
git commit -m "MENSAJE, ARCHIVO.js"

subir cambios a github
git push

nos saldrá un output el cual nos dirá en que línea está lo qué estamos buscando.
git grep -n color 

nos saldrá un output el cual nos dirá cuantas veces se repite esa palabra y en qué archivo.
git grep -c color 

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
